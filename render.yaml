services:
  - type: web
    name: node-red
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    disk:
      name: node-red-data
      mountPath: /data
    envVars:
      - key: NODE_RED_ENABLE_PROJECTS
        value: 'true'
